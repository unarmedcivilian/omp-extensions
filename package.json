{
  "name": "omp-extensions",
  "private": true,
  "type": "module",
  "workspaces": ["extensions/*"],
  "scripts": {
    "test": "bun test tests/*.test.ts && bun --cwd extensions/generative-ui test && bun --cwd extensions/chatgpt-links test",
    "check": "bun test tests/*.test.ts && bun --cwd extensions/generative-ui check && bun --cwd extensions/chatgpt-links check"
  }
}
