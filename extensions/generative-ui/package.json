{
  "name": "omp-generative-ui",
  "version": "0.1.0",
  "description": "Generative UI for omp — render interactive HTML/SVG widgets in cmux browser surfaces",
  "type": "module",
  "keywords": ["omp-package", "oh-my-pi", "generative-ui"],
  "files": ["src", "README.md"],
  "omp": { "extensions": ["./src/index.ts"] },
  "scripts": {
    "build:runtime": "bun src/build.mjs",
    "test": "bun test tests/*.test.ts",
    "check": "bun run build:runtime && bun test tests/*.test.ts && bun build src/index.ts --target=bun --external @oh-my-pi/pi-coding-agent --outdir /tmp/omp-generative-ui-check"
  },
  "peerDependencies": { "@oh-my-pi/pi-coding-agent": "^15", "@oh-my-pi/pi-tui": "^15" },
  "engines": { "bun": ">=1.3.14" },
  "license": "MIT"
}
