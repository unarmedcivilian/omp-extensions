{
  "name": "omp-chatgpt-links",
  "version": "0.1.0",
  "description": "ChatGPT link importer for omp — open conversations in cmux browser and save extracted text",
  "type": "module",
  "keywords": ["omp-package", "oh-my-pi", "chatgpt"],
  "files": ["src", "README.md"],
  "omp": { "extensions": ["./src/index.ts"] },
  "scripts": {
    "test": "bun test tests/*.test.ts",
    "check": "bun test tests/*.test.ts && bun build src/index.ts --target=bun --external @oh-my-pi/pi-coding-agent --outdir /tmp/omp-chatgpt-links-check"
  },
  "peerDependencies": { "@oh-my-pi/pi-coding-agent": "^15" },
  "engines": { "bun": ">=1.3.14" },
  "license": "MIT"
}
